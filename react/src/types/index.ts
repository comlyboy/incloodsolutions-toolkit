/**
 * The build/runtime modes recognised by a Vite app in this ecosystem.
 *
 * Matches the `--mode` flag / `import.meta.env.MODE` value:
 * `'development' | 'qa' | 'staging' | 'production'`.
 */
export type ViteModeType = 'development' | 'qa' | 'staging' | 'production';
